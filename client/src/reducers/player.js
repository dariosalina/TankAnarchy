
const inititalState = {
    position: [0,0]
}

const playerReducer = (state = inititalState, action) => {
    switch(action.type) {
        default:  
            return state
    }
}

export default playerReducer